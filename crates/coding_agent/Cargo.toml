[package]
name = "coding_agent"
version = "0.1.0"
edition = "2021"

[dependencies]
wait-timeout = "0.2"
tape_tui = { path = "../.." }
agent_provider = { path = "../agent_provider" }
agent_provider_mock = { path = "../agent_provider_mock" }

[dev-dependencies]
tempfile = "3"
